# Core dependencies (required)
librosa>=0.10.1
mido>=1.3.0
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.1
click>=8.1.0

# HIL (Hardware-in-the-Loop) dependencies
sounddevice>=0.4.6  # Audio I/O for real-time processing
pyaudio>=0.2.11     # Alternative audio I/O (optional)

# Optional dependencies for enhanced features
pedalboard>=0.7.4  # Audio processing (optional)

# High-performance backend (optional, install separately)
# essentia  # Install via: pip install essentia or conda install -c conda-forge essentia
